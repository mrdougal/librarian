defaults: &defaults
  adapter: sqlite3

development:
  database: librarian_development.db
  <<: *defaults

  # Add more repositories
  # repositories:
  #   repo1:
  #     adapter:  postgresql
  #     database: sample_development
  #     username: the_user
  #     password: secrets
  #     host:     localhost
  #   repo2:
  #     ...

test:
  database: librarian_test.db
  <<: *defaults
production:
  database: librarian_production.db
  <<: *defaults
